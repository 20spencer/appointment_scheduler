package scheduler;

/**
 * ViewBy is to help make the method populating the Appointment TableView
 * easier to sort based on the radiobutton selected.
 */
public enum ViewBy {
    ALL,
    MONTH,
    WEEK
}
